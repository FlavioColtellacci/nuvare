# Local environment files contain secrets. Cursor should not index or include them
# in codebase search — reduces accidental exposure in AI context.
# (.env.example is intentionally NOT listed so templates stay visible.)
.env.local
.env.development.local
.env.production.local
.env.test.local
.env
